-->performs operations on a set of values and gve a single output
ysql> select max(age) from student;
+----------+
| max(age) |
+----------+
|       10 |
+----------+
--> max(),min(),avg();
--<GROUP BY CLAUSE
-- GROUPA ROws that group the rows with same values 
generall  we use with aggreagre fuction 
